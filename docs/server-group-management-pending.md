# 服务器群组管理相关未完成事项记录

## 背景
- 已完成：修复“添加服务器”按钮弹窗行为、SSH 配置导入后服务器列表自动刷新，以及服务器群组图表的 `getCurrentLanguage` 运行时异常。
- 未完成：仓库中仍存在大量旧的 ESLint/Prettier 违规（主要是 CRLF、`any` 类型等），本次仅针对 `server-group-management.ts` 做了整理，其它模块仍需后续集中清理。

## 建议后续行动
1. 计划化处理前端目录的遗留 ESLint/Prettier 问题，确保 `npm run lint` 可在整个项目范围内通过。
2. 若将来继续重构服务器群组模块，建议在修复前先补充集成测试或 E2E 覆盖，避免回归。
