# 阶段4 功能测试指南

**测试目标**: 验证Step 1数据读取切换到Hub的功能完整性  
**测试日期**: 2025-10-17  
**前置条件**: Hub服务和主应用都正常运行

---

## 一、环境准备

### 1.1 依赖服务检查

在启动Hub和主应用之前,确保以下服务正在运行:

| 服务 | 端口 | 检查命令 | 说明 |
|------|------|----------|------|
| **PostgreSQL** | 5432 | `Test-NetConnection localhost -Port 5432` | Hub数据持久化 |
| **Redis** | 6379 | `Test-NetConnection localhost -Port 6379` | Hub热数据缓存 |

#### 启动依赖服务

**选项A: Docker方式** (推荐)
```powershell
# 1. 启动 PostgreSQL (带TimescaleDB)
docker run -d `
  --name metrics-postgres `
  -e POSTGRES_DB=metrics_hub `
  -e POSTGRES_USER=postgres `
  -e POSTGRES_PASSWORD=postgres `
  -p 5432:5432 `
  timescale/timescaledb:latest-pg14

# 2. 启动 Redis
docker run -d `
  --name metrics-redis `
  -p 6379:6379 `
  redis:7-alpine

# 3. 验证服务
docker ps
```

**选项B: 本地安装**
- PostgreSQL: 从官网下载安装,创建数据库 `metrics_hub`
- Redis: 从官网下载安装,使用默认配置

### 1.2 启动Hub服务

```powershell
# 进入Hub目录
cd hub

# 启动Hub (开发模式)
../mvnw.cmd spring-boot:run -Dspring-boot.run.profiles=dev

# 等待启动完成 (约15-30秒)
# 看到 "Started MetricsHubApplication" 表示成功
```

**验证Hub启动**:
```powershell
# 健康检查
curl http://localhost:8080/actuator/health

# 预期输出:
# {"status":"UP","components":{"db":{"status":"UP"},"redis":{"status":"UP"},...}}
```

### 1.3 启动主应用

```powershell
# 返回项目根目录
cd ..

# 启动主应用 (开发模式)
./mvnw.cmd spring-boot:run -Dspring-boot.run.profiles=dev

# 等待启动完成 (约10-20秒)
# 看到 "Started InternalpaasApplication" 表示成功
```

**验证主应用启动**:
```powershell
# 访问首页
curl http://localhost:8081

# 预期: 返回HTML页面
```

---

## 二、功能测试用例

### 2.1 测试场景1: Hub正常 - 单服务器查询

**目标**: 验证从Hub成功获取单台服务器监控数据

**前置条件**:
- Hub服务运行中 (8080端口)
- 主应用运行中 (8081端口)
- 至少有一台服务器已部署Agent并上报数据

**测试步骤**:
```powershell
# 1. 查询服务器列表 (假设有服务器ID=1)
curl http://localhost:8081/api/servers

# 2. 查询服务器监控数据
curl http://localhost:8081/api/monitoring/servers/1

# 3. 检查响应
```

**预期结果**:
- ✅ 状态码: `200 OK`
- ✅ 响应体包含:
  ```json
  {
    "success": true,
    "data": {
      "serverId": 1,
      "timestamp": "2025-10-17T...",
      "cpuUsage": 45.2,
      "memoryUsage": 62.8,
      "diskUsage": 38.5,
      "dataSource": "hub"  // 关键字段,证明来自Hub
    }
  }
  ```
- ✅ 响应时间: < 100ms (Hub优化后)

**失败场景**:
- ❌ 如果返回 `503`: Hub服务不可用 → 检查Hub健康状态
- ❌ 如果返回 `404`: 服务器无监控数据 → 检查Agent是否运行

---

### 2.2 测试场景2: Hub正常 - 批量查询

**目标**: 验证批量获取所有服务器监控数据

**测试步骤**:
```powershell
# 批量查询所有服务器
curl http://localhost:8081/api/monitoring/servers

# 或通过浏览器访问仪表板
start http://localhost:8081/dashboard
```

**预期结果**:
- ✅ 状态码: `200 OK`
- ✅ 响应体包含:
  ```json
  {
    "success": true,
    "data": [
      {"serverId": 1, "cpuUsage": 45.2, ...},
      {"serverId": 2, "cpuUsage": 38.7, ...}
    ],
    "statistics": {
      "totalServers": 2,
      "successCount": 2,  // Hub成功获取
      "errorCount": 0,
      "dataSource": "hub"  // 全部来自Hub
    }
  }
  ```
- ✅ 响应时间: < 500ms (多服务器场景)

---

### 2.3 测试场景3: Hub不可用 - 503错误

**目标**: 验证Hub不可用时返回明确错误(无SSH降级)

**测试步骤**:
```powershell
# 1. 停止Hub服务
# (在Hub终端按 Ctrl+C)

# 2. 等待5秒让健康检查失效

# 3. 查询监控数据
curl http://localhost:8081/api/monitoring/servers/1
```

**预期结果**:
- ✅ 状态码: `503 Service Unavailable`
- ✅ 响应体:
  ```json
  {
    "success": false,
    "error": {
      "code": "HUB_UNAVAILABLE",
      "message": "监控服务暂时不可用,请稍后重试",
      "troubleshooting": [
        "检查Hub服务是否正常运行",
        "查看Hub日志: logs/metrics-hub.log",
        "参考文档: doc/operations-manual.md"
      ]
    }
  }
  ```
- ✅ **关键验证**: ❌ 不会返回SSH数据 (降级逻辑已移除)

---

### 2.4 测试场景4: Agent未部署 - 404错误

**目标**: 验证服务器无监控数据时的错误提示

**测试步骤**:
```powershell
# 查询一个未部署Agent的服务器 (假设ID=999)
curl http://localhost:8081/api/monitoring/servers/999
```

**预期结果**:
- ✅ 状态码: `404 Not Found`
- ✅ 响应体:
  ```json
  {
    "success": false,
    "error": {
      "code": "NO_METRICS_DATA",
      "message": "服务器 999 暂无监控数据",
      "suggestions": [
        "检查Agent是否已部署",
        "检查Agent是否正常运行",
        "查看Agent日志确认数据上报",
        "等待1-2分钟后重试"
      ]
    }
  }
  ```

---

### 2.5 测试场景5: Agent部署后数据验证

**目标**: 验证Agent部署后自动验证Hub数据上报

**测试步骤**:
```powershell
# 1. 部署Agent到新服务器
curl -X POST http://localhost:8081/api/agents/deploy `
  -H "Content-Type: application/json" `
  -d '{
    "serverId": 5,
    "agentVersion": "latest"
  }'

# 2. 观察部署日志
```

**预期结果**:
- ✅ 部署过程包含Hub验证步骤:
  ```
  [INFO] 正在部署Agent到服务器 5...
  [INFO] Agent安装完成
  [INFO] 正在验证Hub数据上报...
  [INFO] 尝试 1/6: 等待Hub数据...
  [INFO] 尝试 2/6: 等待Hub数据...
  [INFO] ✅ Hub数据验证成功! 数据时间: 2025-10-17T10:30:45Z
  [INFO] 部署完成
  ```
- ✅ 验证配置:
  - 最多6次重试
  - 每次间隔5秒
  - 数据新鲜度: 60秒内
  - 总超时: 30秒

---

## 三、性能测试

### 3.1 响应时间对比

**测试工具**: Apache Bench (ab) 或 curl

**测试1: 单服务器查询**
```powershell
# 执行100次请求
for ($i=1; $i -le 100; $i++) {
    $start = Get-Date
    curl -s http://localhost:8081/api/monitoring/servers/1 | Out-Null
    $duration = (Get-Date) - $start
    Write-Host "请求 $i : $($duration.TotalMilliseconds) ms"
}
```

**预期结果**:
- 平均响应时间: **< 100ms** (Hub优先)
- 对比旧版本: **降低 33-95%**
- P95延迟: < 150ms

**测试2: 批量查询 (10台服务器)**
```powershell
Measure-Command {
    curl -s http://localhost:8081/api/monitoring/servers | Out-Null
}
```

**预期结果**:
- 总响应时间: **< 500ms**
- 对比旧版本: **降低 50-80%**

### 3.2 吞吐量测试

**测试工具**: JMeter 或 Gatling

**配置**:
- 并发用户: 50
- 持续时间: 60秒
- 目标: `/api/monitoring/servers`

**预期结果**:
- **QPS**: 500-1000 请求/秒
- **错误率**: < 1%
- **CPU使用率**: < 60%
- **内存使用**: < 1GB

---

## 四、集成测试

### 4.1 前端仪表板测试

**测试步骤**:
```powershell
# 1. 访问仪表板
start http://localhost:8081/dashboard

# 2. 观察以下功能
```

**检查项**:
- ✅ 服务器列表正常显示
- ✅ 监控图表实时更新 (每10秒刷新)
- ✅ CPU/内存/磁盘使用率准确
- ✅ 数据来源标识为 "Hub"
- ✅ Hub不可用时显示错误提示 (而非旧数据)

### 4.2 监控告警测试

**测试场景**: CPU使用率超过阈值

**测试步骤**:
```powershell
# 1. 模拟高CPU负载 (在目标服务器上)
# Linux: stress --cpu 4 --timeout 60s
# Windows: start /low powershell -c "while(1){1+1}"

# 2. 观察告警触发
```

**预期结果**:
- ✅ 在60秒内检测到CPU飙升
- ✅ 触发告警通知
- ✅ 数据来自Hub (非SSH轮询)

---

## 五、兼容性测试

### 5.1 API响应格式变化

**重要变更**: 批量查询响应格式

**旧格式**:
```json
{
  "data": [...],
  "dataSourceStats": {
    "hub": 5,
    "ssh": 3
  }
}
```

**新格式**:
```json
{
  "data": [...],
  "statistics": {
    "successCount": 8,
    "errorCount": 0,
    "dataSource": "hub"
  }
}
```

**测试步骤**:
```powershell
# 检查前端是否正确解析新格式
curl http://localhost:8081/api/monitoring/servers | ConvertFrom-Json
```

**预期结果**:
- ✅ 前端正常显示数据
- ✅ 不再显示 "数据来源: SSH" 标签

### 5.2 错误处理兼容性

**测试**: 前端如何处理503错误

**测试步骤**:
```powershell
# 1. 停止Hub
# 2. 刷新仪表板页面
```

**预期结果**:
- ✅ 显示友好错误提示: "监控服务暂时不可用"
- ✅ 不显示旧数据 (可能误导)
- ✅ 提供重试按钮

---

## 六、回归测试

### 6.1 核心功能验证

| 功能模块 | 测试内容 | 状态 |
|---------|---------|------|
| **服务器管理** | 添加/编辑/删除服务器 | ⬜ 待测 |
| **Agent部署** | 自动部署Agent + Hub验证 | ⬜ 待测 |
| **监控查询** | 单服务器/批量查询 | ⬜ 待测 |
| **仪表板** | 实时数据展示 | ⬜ 待测 |
| **告警** | CPU/内存/磁盘告警 | ⬜ 待测 |
| **权限** | 用户登录/权限控制 | ⬜ 待测 |

### 6.2 非功能性测试

| 测试项 | 指标 | 目标 | 状态 |
|-------|------|------|------|
| **响应时间** | 单服务器查询 | < 100ms | ⬜ 待测 |
| **吞吐量** | 批量查询QPS | 500-1000 | ⬜ 待测 |
| **稳定性** | 连续运行24小时 | 无内存泄漏 | ⬜ 待测 |
| **错误率** | Hub不可用场景 | < 1% | ⬜ 待测 |

---

## 七、测试结果记录

### 7.1 测试执行记录表

| 测试用例 | 执行时间 | 结果 | 备注 |
|---------|---------|------|------|
| 2.1 单服务器查询 | | | |
| 2.2 批量查询 | | | |
| 2.3 Hub不可用 | | | |
| 2.4 Agent未部署 | | | |
| 2.5 Agent部署验证 | | | |
| 3.1 响应时间测试 | | | |
| 3.2 吞吐量测试 | | | |
| 4.1 前端仪表板 | | | |
| 4.2 监控告警 | | | |

### 7.2 问题记录表

| 问题编号 | 问题描述 | 严重程度 | 状态 | 负责人 |
|---------|---------|---------|------|-------|
| | | | | |

### 7.3 性能指标记录

**响应时间**:
- 单服务器查询平均: _____ ms
- 批量查询平均: _____ ms
- P95延迟: _____ ms

**吞吐量**:
- QPS峰值: _____ 请求/秒
- 错误率: _____ %

**资源使用**:
- Hub CPU: _____ %
- Hub 内存: _____ MB
- 主应用 CPU: _____ %
- 主应用 内存: _____ MB

---

## 八、测试通过标准

### 8.1 必须通过的测试

- ✅ **功能测试**: 所有场景2.1-2.5通过
- ✅ **性能测试**: 响应时间达标 (< 100ms单查询, < 500ms批量)
- ✅ **兼容性测试**: 前端正常工作,API格式正确
- ✅ **错误处理**: Hub不可用时正确返回503,无SSH降级

### 8.2 阻断性问题

以下问题必须修复才能进入Step 2:

- ❌ **P0**: Hub正常但无法获取数据
- ❌ **P0**: 返回503以外的错误码 (如500)
- ❌ **P0**: 仍然触发SSH降级逻辑
- ❌ **P1**: 响应时间超过200ms
- ❌ **P1**: 前端无法解析新API格式

---

## 九、快速启动脚本

为了简化测试流程,创建以下启动脚本:

**start-test-env.ps1**:
```powershell
# 启动测试环境
Write-Host "========================================" -ForegroundColor Cyan
Write-Host "  阶段4 功能测试环境启动脚本" -ForegroundColor Cyan
Write-Host "========================================" -ForegroundColor Cyan

# 1. 检查依赖服务
Write-Host "`n[1/4] 检查依赖服务..." -ForegroundColor Yellow
$postgresUp = Test-NetConnection localhost -Port 5432 -InformationLevel Quiet
$redisUp = Test-NetConnection localhost -Port 6379 -InformationLevel Quiet

if (-not $postgresUp) {
    Write-Host "  ❌ PostgreSQL未运行 (5432)" -ForegroundColor Red
    Write-Host "  提示: docker run -d --name metrics-postgres -e POSTGRES_DB=metrics_hub -e POSTGRES_PASSWORD=postgres -p 5432:5432 timescale/timescaledb:latest-pg14"
    exit 1
}
Write-Host "  ✅ PostgreSQL已运行" -ForegroundColor Green

if (-not $redisUp) {
    Write-Host "  ❌ Redis未运行 (6379)" -ForegroundColor Red
    Write-Host "  提示: docker run -d --name metrics-redis -p 6379:6379 redis:7-alpine"
    exit 1
}
Write-Host "  ✅ Redis已运行" -ForegroundColor Green

# 2. 启动Hub
Write-Host "`n[2/4] 启动Hub服务..." -ForegroundColor Yellow
Start-Job -Name "hub-service" -ScriptBlock {
    Set-Location $using:PWD\hub
    ..\mvnw.cmd spring-boot:run -Dspring-boot.run.profiles=dev
} | Out-Null
Write-Host "  ⏳ Hub启动中,等待30秒..." -ForegroundColor Cyan
Start-Sleep -Seconds 30

# 3. 验证Hub
$hubHealth = $false
for ($i = 1; $i -le 5; $i++) {
    try {
        $response = Invoke-RestMethod -Uri "http://localhost:8080/actuator/health" -ErrorAction SilentlyContinue
        if ($response.status -eq "UP") {
            $hubHealth = $true
            break
        }
    } catch {}
    Start-Sleep -Seconds 5
}

if (-not $hubHealth) {
    Write-Host "  ❌ Hub健康检查失败" -ForegroundColor Red
    exit 1
}
Write-Host "  ✅ Hub已就绪 (http://localhost:8080)" -ForegroundColor Green

# 4. 启动主应用
Write-Host "`n[3/4] 启动主应用..." -ForegroundColor Yellow
Start-Job -Name "main-app" -ScriptBlock {
    Set-Location $using:PWD
    .\mvnw.cmd spring-boot:run -Dspring-boot.run.profiles=dev
} | Out-Null
Write-Host "  ⏳ 主应用启动中,等待20秒..." -ForegroundColor Cyan
Start-Sleep -Seconds 20

# 5. 验证主应用
$appHealth = $false
for ($i = 1; $i -le 5; $i++) {
    try {
        Invoke-WebRequest -Uri "http://localhost:8081" -UseBasicParsing -ErrorAction SilentlyContinue | Out-Null
        $appHealth = $true
        break
    } catch {}
    Start-Sleep -Seconds 5
}

if (-not $appHealth) {
    Write-Host "  ❌ 主应用启动失败" -ForegroundColor Red
    exit 1
}
Write-Host "  ✅ 主应用已就绪 (http://localhost:8081)" -ForegroundColor Green

# 6. 测试总结
Write-Host "`n========================================" -ForegroundColor Cyan
Write-Host "  ✅ 测试环境启动成功!" -ForegroundColor Green
Write-Host "========================================" -ForegroundColor Cyan
Write-Host ""
Write-Host "服务地址:" -ForegroundColor Yellow
Write-Host "  - Hub服务:  http://localhost:8080"
Write-Host "  - 主应用:   http://localhost:8081"
Write-Host "  - 仪表板:   http://localhost:8081/dashboard"
Write-Host ""
Write-Host "日志位置:" -ForegroundColor Yellow
Write-Host "  - Hub:  hub/logs/metrics-hub.log"
Write-Host "  - 主应用: logs/internalpaas.log"
Write-Host ""
Write-Host "开始测试: 请参考 doc/阶段4-功能测试指南.md" -ForegroundColor Cyan
Write-Host ""
```

**stop-test-env.ps1**:
```powershell
# 停止测试环境
Write-Host "停止测试环境..." -ForegroundColor Yellow
Get-Job | Stop-Job
Get-Job | Remove-Job
Write-Host "✅ 已停止所有服务" -ForegroundColor Green
```

---

## 十、下一步

完成所有测试后:

1. ✅ **通过**: 填写测试报告 → 进入 **Step 2** (停用SSH监控任务)
2. ❌ **失败**: 记录问题 → 修复Bug → 重新测试

**Step 2 预览**:
- 停用 `ServerMetricsScheduler` 定时任务
- 移除 `server_metrics` 表写入逻辑
- 预计耗时: 0.5天

---

**测试执行人**: __________  
**测试日期**: __________  
**测试结果**: ⬜ 通过 / ⬜ 失败  
**签名**: __________
